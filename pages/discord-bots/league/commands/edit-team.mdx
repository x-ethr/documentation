# `/edit-team`

Change your team's name or color.

## Options

- `name` `String` optional
- `color` `String` optional

## Notes

- This is a captain-facing command
- Color must be a hex code such as `#FFFFFF`
- The command enforces the bot's team editing rules in code, including cooldown logic
