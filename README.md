# Heir Documentation

Docs for [Heir](https://heir.gg) — Competition as a Service.

```bash
npm install
npm run dev
```

Built with [Nextra](https://nextra.site). Pages live in `pages/`; navigation and
sidebar grouping are defined by the `_meta.json` file in each directory.
